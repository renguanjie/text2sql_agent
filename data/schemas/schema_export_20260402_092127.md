---
title: 数据库 Schema 文档
generated_at: 2026-04-02 09:21:27
database_count: 2
table_count: 60
column_count: 0
---

# 📚 数据库 Schema 文档

## 目录

1. [数据库概览](#数据库概览)
2. [业务概念](#业务概念)
3. [default](#default)
3. [ride_hailing_db](#ride_hailing_db)

## 数据库概览

| 数据库 ID | 数据库名称 | 类型 | 语言 | 描述 |
|----------|----------|------|------|------|
| 1 | default | mysql | SQL | None |
| 2 | ride_hailing_db | sparksql | SQL | None |

## 业务概念

暂无业务概念

---

## default

**数据库 ID**: 1
**类型**: mysql
**语言**: SQL
**描述**: None

*暂无表数据*

---

## ride_hailing_db

**数据库 ID**: 2
**类型**: sparksql
**语言**: SQL
**描述**: None

### 表列表 (共 30 张)

#### `activity_main`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE activity_main (
    activity_id BIGINT COMMENT '活动ID',
    activity_name STRING COMMENT '活动名称',
    activity_type TINYINT COMMENT '活动类型 1-新用户立减 2-满减 3-折扣 4-抽奖',
    city_id INT COMMENT '适用城市ID',
    start_time TIMESTAMP COMMENT '活动开始时间',
    end_time TIMESTAMP COMMENT '活动结束时间',
    activity_status TINYINT COMMENT '活动状态 0-未开始 1-进行中 2-已结束',
    budget DECIMAL(18,2) COMMENT '活动预算',
    rule_desc STRING COMMENT '活动规则描述',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '营销活动主表';
```


#### `activity_user_join`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE activity_user_join (
    join_id BIGINT COMMENT '参与ID',
    activity_id BIGINT COMMENT '活动ID',
    user_id BIGINT COMMENT '用户ID',
    join_time TIMESTAMP COMMENT '参与时间',
    get_reward TINYINT COMMENT '是否获得奖励 1-是 0-否',
    reward_amount DECIMAL(18,2) COMMENT '奖励金额',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户参与活动记录表';
```


#### `bas_user_address`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE bas_user_address (
    addr_id BIGINT COMMENT '地址ID',
    user_id BIGINT COMMENT '关联用户ID',
    addr_name STRING COMMENT '地址名称 家/公司/其他',
    province STRING COMMENT '省份',
    city STRING COMMENT '城市',
    district STRING COMMENT '区县',
    detail_addr STRING COMMENT '详细地址',
    longitude DECIMAL(10,6) COMMENT '经度',
    latitude DECIMAL(10,6) COMMENT '纬度',
    is_default TINYINT COMMENT '是否默认地址 1-是 0-否',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户常用地址表';
```


#### `bas_user_auth`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE bas_user_auth (
    auth_id BIGINT COMMENT '认证记录ID',
    user_id BIGINT COMMENT '关联用户ID',
    auth_type TINYINT COMMENT '认证类型 1-实名认证 2-人脸认证 3-学生认证',
    auth_status TINYINT COMMENT '认证状态 0-待审核 1-通过 2-驳回',
    auth_time TIMESTAMP COMMENT '认证完成时间',
    fail_reason STRING COMMENT '认证失败原因',
    operator_id BIGINT COMMENT '审核操作员ID',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户身份认证记录表';
```


#### `bas_user_info`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE bas_user_info (
    user_id BIGINT COMMENT '用户唯一ID',
    user_name STRING COMMENT '用户姓名',
    phone STRING COMMENT '用户手机号',
    id_card STRING COMMENT '身份证号',
    gender TINYINT COMMENT '性别 1-男 2-女 0-未知',
    birth_date DATE COMMENT '出生日期',
    register_time TIMESTAMP COMMENT '注册时间',
    user_status TINYINT COMMENT '用户状态 0-注销 1-正常 2-冻结',
    city_id INT COMMENT '注册城市ID',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户基础信息表';
```


#### `complaint_driver`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE complaint_driver (
    complaint_id BIGINT COMMENT '投诉ID',
    order_id BIGINT COMMENT '关联订单ID',
    user_id BIGINT COMMENT '投诉用户ID',
    driver_id BIGINT COMMENT '被投诉司机ID',
    complaint_type TINYINT COMMENT '投诉类型 1-拒载 2-绕路 3-服务差 4-其他',
    complaint_content STRING COMMENT '投诉内容',
    complaint_time TIMESTAMP COMMENT '投诉时间',
    audit_status TINYINT COMMENT '审核状态 0-待审核 1-有效 2-无效',
    handle_result STRING COMMENT '处理结果',
    handle_time TIMESTAMP COMMENT '处理时间',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户投诉司机记录表';
```


#### `complaint_user`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE complaint_user (
    complaint_id BIGINT COMMENT '投诉ID',
    order_id BIGINT COMMENT '关联订单ID',
    driver_id BIGINT COMMENT '投诉司机ID',
    user_id BIGINT COMMENT '被投诉用户ID',
    complaint_type TINYINT COMMENT '投诉类型 1-爽约 2-恶意取消 3-辱骂 4-其他',
    complaint_content STRING COMMENT '投诉内容',
    complaint_time TIMESTAMP COMMENT '投诉时间',
    audit_status TINYINT COMMENT '审核状态 0-待审核 1-有效 2-无效',
    handle_result STRING COMMENT '处理结果',
    handle_time TIMESTAMP COMMENT '处理时间',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '司机投诉用户记录表';
```


#### `coupon_main`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE coupon_main (
    coupon_id BIGINT COMMENT '优惠券ID',
    coupon_name STRING COMMENT '优惠券名称',
    coupon_type TINYINT COMMENT '优惠券类型 1-满减 2-立减 3-折扣',
    condition_amount DECIMAL(18,2) COMMENT '使用门槛(满X元)',
    discount_amount DECIMAL(18,2) COMMENT '优惠金额/折扣',
    valid_day INT COMMENT '有效期(天)',
    status TINYINT COMMENT '状态 1-启用 0-停用',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '优惠券主表';
```


#### `coupon_user_receive`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE coupon_user_receive (
    receive_id BIGINT COMMENT '领取ID',
    user_id BIGINT COMMENT '用户ID',
    coupon_id BIGINT COMMENT '优惠券ID',
    activity_id BIGINT COMMENT '关联活动ID',
    receive_time TIMESTAMP COMMENT '领取时间',
    expire_time DATE COMMENT '过期时间',
    status TINYINT COMMENT '状态 0-未使用 1-已使用 2-已过期',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户优惠券领取记录表';
```


#### `coupon_user_use`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE coupon_user_use (
    use_id BIGINT COMMENT '使用ID',
    receive_id BIGINT COMMENT '领取记录ID',
    user_id BIGINT COMMENT '用户ID',
    order_id BIGINT COMMENT '关联订单ID',
    use_time TIMESTAMP COMMENT '使用时间',
    discount_amount DECIMAL(18,2) COMMENT '抵扣金额',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户优惠券使用记录表';
```


#### `driver_auth`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE driver_auth (
    auth_id BIGINT COMMENT '认证ID',
    driver_id BIGINT COMMENT '关联司机ID',
    driving_license_no STRING COMMENT '驾驶证号',
    driving_license_expire DATE COMMENT '驾驶证有效期',
    qualification_no STRING COMMENT '从业资格证号',
    qualification_expire DATE COMMENT '资格证有效期',
    auth_status TINYINT COMMENT '认证状态 0-待审核 1-通过 2-驳回',
    auth_time TIMESTAMP COMMENT '认证时间',
    operator_id BIGINT COMMENT '审核员ID',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '司机资质认证表';
```


#### `driver_car_info`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE driver_car_info (
    car_id BIGINT COMMENT '车辆ID',
    driver_id BIGINT COMMENT '关联司机ID',
    car_no STRING COMMENT '车牌号',
    car_type TINYINT COMMENT '车型 1-经济型 2-舒适型 3-商务型',
    car_brand STRING COMMENT '车辆品牌',
    car_model STRING COMMENT '车辆型号',
    car_color STRING COMMENT '车辆颜色',
    car_age INT COMMENT '车龄(年)',
    car_insure_expire DATE COMMENT '保险到期日',
    car_check_expire DATE COMMENT '年检到期日',
    car_status TINYINT COMMENT '车辆状态 1-正常 2-停用',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '司机车辆信息表';
```


#### `driver_info`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE driver_info (
    driver_id BIGINT COMMENT '司机唯一ID',
    driver_name STRING COMMENT '司机姓名',
    phone STRING COMMENT '司机手机号',
    id_card STRING COMMENT '身份证号',
    gender TINYINT COMMENT '性别 1-男 2-女 0-未知',
    birth_date DATE COMMENT '出生日期',
    register_time TIMESTAMP COMMENT '注册时间',
    driver_status TINYINT COMMENT '司机状态 0-注销 1-正常 2-冻结 3-休息',
    city_id INT COMMENT '服务城市ID',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '司机基础信息表';
```


#### `driver_receive_order`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE driver_receive_order (
    receive_id BIGINT COMMENT '接单ID',
    order_id BIGINT COMMENT '订单ID',
    driver_id BIGINT COMMENT '司机ID',
    receive_time TIMESTAMP COMMENT '接单时间',
    driver_lng DECIMAL(10,6) COMMENT '接单时司机经度',
    driver_lat DECIMAL(10,6) COMMENT '接单时司机纬度',
    distance_to_start DECIMAL(18,2) COMMENT '司机到起点距离(公里)',
    expect_pickup_time INT COMMENT '预计接驾时间(分钟)',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '司机接单记录表';
```


#### `driver_score`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE driver_score (
    score_id BIGINT COMMENT '评分ID',
    driver_id BIGINT COMMENT '关联司机ID',
    score_date DATE COMMENT '统计日期',
    service_score DECIMAL(3,1) COMMENT '服务分',
    position_score DECIMAL(3,1) COMMENT '位置准确率',
    cancel_rate DECIMAL(5,2) COMMENT '取消率%',
    complaint_count INT COMMENT '投诉量',
    total_score DECIMAL(3,1) COMMENT '综合评分',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '司机评分统计表';
```


#### `driver_withdraw`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE driver_withdraw (
    withdraw_id BIGINT COMMENT '提现ID',
    driver_id BIGINT COMMENT '关联司机ID',
    withdraw_amount DECIMAL(18,2) COMMENT '提现金额',
    withdraw_time TIMESTAMP COMMENT '提现时间',
    account_no STRING COMMENT '收款账户号',
    account_name STRING COMMENT '收款人姓名',
    withdraw_status TINYINT COMMENT '提现状态 0-处理中 1-成功 2-失败',
    fee DECIMAL(18,2) COMMENT '提现手续费',
    arrive_time TIMESTAMP COMMENT '到账时间',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '司机提现记录表';
```


#### `driver_work_daily`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE driver_work_daily (
    work_id BIGINT COMMENT '记录ID',
    driver_id BIGINT COMMENT '关联司机ID',
    work_date DATE COMMENT '工作日期',
    online_hour DECIMAL(5,2) COMMENT '在线时长(小时)',
    receive_order_count INT COMMENT '接单量',
    finish_order_count INT COMMENT '完成订单量',
    cancel_order_count INT COMMENT '取消订单量',
    total_income DECIMAL(18,2) COMMENT '当日总收入',
    total_mileage DECIMAL(18,2) COMMENT '当日总里程(公里)',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '司机每日工作统计表';
```


#### `express_car_config`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE express_car_config (
    car_type_id TINYINT COMMENT '车型ID',
    car_type_name STRING COMMENT '车型名称',
    seat_count INT COMMENT '座位数',
    min_start_price DECIMAL(18,2) COMMENT '起步价',
    max_passenger INT COMMENT '最大乘客数',
    car_brand_limit STRING COMMENT '品牌限制',
    car_age_limit INT COMMENT '最大车龄限制',
    status TINYINT COMMENT '状态 1-启用 0-停用',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '快车车型配置表';
```


#### `express_carpool_order`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE express_carpool_order (
    carpool_id BIGINT COMMENT '拼车ID',
    group_id BIGINT COMMENT '拼车组ID',
    order_id BIGINT COMMENT '子订单ID',
    user_id BIGINT COMMENT '用户ID',
    passenger_count INT COMMENT '乘客数',
    share_discount DECIMAL(18,2) COMMENT '拼车折扣',
    share_status TINYINT COMMENT '拼车状态 1-匹配中 2-已拼成 3-已取消',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '拼车订单记录表';
```


#### `express_hitch_order`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE express_hitch_order (
    hitch_id BIGINT COMMENT '顺路单ID',
    order_id BIGINT COMMENT '订单ID',
    driver_id BIGINT COMMENT '司机ID',
    driver_route STRING COMMENT '司机常跑路线',
    hitch_type TINYINT COMMENT '顺路单类型 1-司机发布 2-用户发布',
    hitch_price DECIMAL(18,2) COMMENT '顺路单价格',
    publish_time TIMESTAMP COMMENT '发布时间',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '顺路单记录表';
```


#### `express_price_config`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE express_price_config (
    price_id BIGINT COMMENT '价格配置ID',
    city_id INT COMMENT '城市ID',
    car_type TINYINT COMMENT '车型',
    time_type TINYINT COMMENT '时段类型 1-平峰 2-高峰 3-夜间',
    start_price DECIMAL(18,2) COMMENT '起步价',
    per_km_price DECIMAL(18,2) COMMENT '每公里价格',
    per_minute_price DECIMAL(18,2) COMMENT '每分钟价格',
    peak_multiplier DECIMAL(3,2) COMMENT '高峰倍数',
    status TINYINT COMMENT '状态 1-启用 0-停用',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '快车价格配置表';
```


#### `order_cancel`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE order_cancel (
    cancel_id BIGINT COMMENT '取消ID',
    order_id BIGINT COMMENT '关联订单ID',
    cancel_user_type TINYINT COMMENT '取消方 1-用户 2-司机 3-平台',
    cancel_time TIMESTAMP COMMENT '取消时间',
    cancel_reason STRING COMMENT '取消原因',
    cancel_fee DECIMAL(18,2) COMMENT '取消费用',
    is_charge TINYINT COMMENT '是否扣费 1-是 0-否',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '订单取消记录表';
```


#### `order_evaluation`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE order_evaluation (
    eval_id BIGINT COMMENT '评价ID',
    order_id BIGINT COMMENT '关联订单ID',
    user_id BIGINT COMMENT '用户ID',
    driver_id BIGINT COMMENT '司机ID',
    total_score TINYINT COMMENT '总体评分 1-5分',
    driver_score TINYINT COMMENT '司机服务评分',
    car_score TINYINT COMMENT '车辆环境评分',
    efficiency_score TINYINT COMMENT '接驾效率评分',
    comment STRING COMMENT '评价内容',
    is_anonymous TINYINT COMMENT '是否匿名 1-是 0-否',
    eval_time TIMESTAMP COMMENT '评价时间',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '订单评价表';
```


#### `order_main`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE order_main (
    order_id BIGINT COMMENT '订单ID',
    order_no STRING COMMENT '订单编号',
    user_id BIGINT COMMENT '用户ID',
    driver_id BIGINT COMMENT '司机ID',
    city_id INT COMMENT '城市ID',
    order_type TINYINT COMMENT '订单类型 1-快车 2-拼车 3-顺路单',
    start_addr STRING COMMENT '起点地址',
    end_addr STRING COMMENT '终点地址',
    start_lng DECIMAL(10,6) COMMENT '起点经度',
    start_lat DECIMAL(10,6) COMMENT '起点纬度',
    end_lng DECIMAL(10,6) COMMENT '终点经度',
    end_lat DECIMAL(10,6) COMMENT '终点纬度',
    start_time TIMESTAMP COMMENT '下单时间',
    expect_finish_time TIMESTAMP COMMENT '预计完成时间',
    actual_finish_time TIMESTAMP COMMENT '实际完成时间',
    order_status TINYINT COMMENT '订单状态 0-待接单 1-已接单 2-行程中 3-已完成 4-已取消',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
CLUSTERED BY (order_id) INTO 100 BUCKETS
COMMENT '订单主表';
```


#### `order_payment`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE order_payment (
    pay_id BIGINT COMMENT '支付ID',
    order_id BIGINT COMMENT '关联订单ID',
    user_id BIGINT COMMENT '用户ID',
    pay_amount DECIMAL(18,2) COMMENT '支付总金额',
    base_price DECIMAL(18,2) COMMENT '基础车费',
    distance_price DECIMAL(18,2) COMMENT '里程费',
    time_price DECIMAL(18,2) COMMENT '时长费',
    coupon_discount DECIMAL(18,2) COMMENT '优惠券抵扣',
    activity_discount DECIMAL(18,2) COMMENT '活动抵扣',
    pay_type TINYINT COMMENT '支付方式 1-微信 2-支付宝 3-余额',
    pay_time TIMESTAMP COMMENT '支付时间',
    pay_status TINYINT COMMENT '支付状态 0-待支付 1-已支付',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '订单支付记录表';
```


#### `order_price_adjust`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE order_price_adjust (
    adjust_id BIGINT COMMENT '调价ID',
    order_id BIGINT COMMENT '关联订单ID',
    adjust_type TINYINT COMMENT '调价类型 1-加价 2-降价',
    old_price DECIMAL(18,2) COMMENT '调整前价格',
    new_price DECIMAL(18,2) COMMENT '调整后价格',
    adjust_reason STRING COMMENT '调价原因 高峰/调度等',
    adjust_time TIMESTAMP COMMENT '调价时间',
    operator_id BIGINT COMMENT '操作员ID',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '订单加价/调价记录表';
```


#### `order_trip`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE order_trip (
    trip_id BIGINT COMMENT '行程ID',
    order_id BIGINT COMMENT '关联订单ID',
    pick_up_time TIMESTAMP COMMENT '接到乘客时间',
    finish_time TIMESTAMP COMMENT '行程结束时间',
    total_mileage DECIMAL(18,2) COMMENT '总里程(公里)',
    wait_time INT COMMENT '等待乘客时间(分钟)',
    drive_time INT COMMENT '行驶时间(分钟)',
    speed_avg DECIMAL(5,2) COMMENT '平均速度(km/h)',
    route_gps STRING COMMENT 'GPS轨迹点',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '订单行程记录表';
```


#### `platform_appeal`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE platform_appeal (
    appeal_id BIGINT COMMENT '申诉ID',
    complaint_id BIGINT COMMENT '关联投诉ID',
    appellant_type TINYINT COMMENT '申诉方 1-用户 2-司机',
    appellant_id BIGINT COMMENT '申诉人ID',
    appeal_content STRING COMMENT '申诉内容',
    appeal_time TIMESTAMP COMMENT '申诉时间',
    audit_status TINYINT COMMENT '审核状态 0-待审核 1-通过 2-驳回',
    audit_result STRING COMMENT '审核结果',
    audit_time TIMESTAMP COMMENT '审核时间',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '平台申诉记录表';
```


#### `user_preference`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE user_preference (
    pref_id BIGINT COMMENT '配置ID',
    user_id BIGINT COMMENT '关联用户ID',
    prefer_car_type TINYINT COMMENT '偏好车型 1-经济型 2-舒适型 3-商务型',
    prefer_pickup_time INT COMMENT '偏好接单时间(分钟)',
    prefer_no_smoke TINYINT COMMENT '是否偏好无烟车 1-是 0-否',
    prefer_shared TINYINT COMMENT '是否偏好拼车 1-是 0-否',
    usual_route STRING COMMENT '常用路线',
    update_time TIMESTAMP COMMENT '更新时间',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户出行喜好配置表';
```


#### `user_tag`

**描述**: None

*暂无字段信息*

**创建语句**:

```sql
CREATE TABLE user_tag (
    tag_id BIGINT COMMENT '标签ID',
    user_id BIGINT COMMENT '关联用户ID',
    tag_code STRING COMMENT '标签编码',
    tag_name STRING COMMENT '标签名称',
    tag_type TINYINT COMMENT '标签类型 1-行为标签 2-属性标签 3-营销标签',
    create_time TIMESTAMP COMMENT '标签生成时间',
    expire_time DATE COMMENT '标签过期时间',
    dt DATE COMMENT '分区日期'
)
USING parquet
PARTITIONED BY (dt)
COMMENT '用户画像标签表';
```


---

*此文档由 Schema Export Tool 自动生成*